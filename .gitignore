*.log
*.log.err
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# --- Frontend (Next.js source in frontend/) ---
/frontend/node_modules
/frontend/.next
/frontend/out

# --- Build output ---
# public/ is a build target: build-frontend.ps1 fills it with the Next.js
# static export. Track only Laravel's own files here; everything else is
# regenerated by the build.
/public/*
!/public/.htaccess
!/public/index.php
!/public/robots.txt
