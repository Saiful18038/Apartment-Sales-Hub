*.log
*.log.err
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/storage/*.key
/storage/license
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Internal operator guide (kill-switch bypass steps + key fingerprint) —
# never push to the public repo; it lives with the key backups on the
# offline drive instead.
/LICENSE-KILL-SWITCH.md

# --- Frontend (Next.js source in frontend/) ---
/frontend/node_modules
/frontend/.next
/frontend/out

# --- Build output ---
# public/ is a build target: build-frontend.ps1 fills it with the Next.js
# static export. Track only Laravel's own files here; everything else is
# regenerated by the build.
/public/*
!/public/.htaccess
!/public/index.php
!/public/robots.txt
